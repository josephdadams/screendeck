// src/defaults.ts

export const defaultSettings = {
    companionIP: '127.0.0.1',
    companionPort: 16622,
    showOnStartup: true,
    deviceIds: [],
}

export type SettingsType = typeof defaultSettings
